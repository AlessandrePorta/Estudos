package _1PastaDeEstudos;

public class ModificadoresDeAcesso {
    /*
    private = membro privado, so pode ser acessado na mesma classe.

    sem modificador = so pode ser acessado pelas classes de um mesmo pacote.

    protected = pode ser acessado apenas no mesmo pacote.

    public = pode ser acessado por qualquer classe
     */
}
